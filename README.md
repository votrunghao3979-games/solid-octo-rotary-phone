# solid-octo-rotary-phone
import random  # Danh sách từ mẫu words = ["apple", "grape", "mango", "peach", "lemon"] secret_word = random.choice(words)  attempts = 6  print("🎮 Chào mừng đến với Word Key!") print("Bạn có 6 lần đoán từ (5 chữ cái).")  while attempts > 0:     guess = input("Nhập từ của bạn: ").lower()          if len(guess) != 500$ 
